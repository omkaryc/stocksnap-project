server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stocksnapdb?useSSL=false&serverTimezone=UTC
    username: root
    password: omkar123   # change if your MySQL password is different
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # auto create tables
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

app:
  jwt:
    secret: 12345678901234567890123456789012stocksnapsecretkey
    expiration-ms: 86400000